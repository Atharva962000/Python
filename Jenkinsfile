pipeline {
  agent any
  stages {  
    stage('Deploy')
    {
      steps {
        echo "deploying the application"
        sh "whoami"
        sh "whoami"
        sh "./bash5.sh"  
      }
    }
  }
}
